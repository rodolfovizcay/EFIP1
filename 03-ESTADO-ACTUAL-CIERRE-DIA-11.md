# Estado actual al cierre del Día 11

**Fecha:** 18/08/2026<br>
**Estado:** **CERRADO Y APROBADO**<br>
**Evaluación:** **9,2/10**<br>
**Recuperación oral de excepciones:** completada

## Dominio incorporado

- arreglo frente a `ArrayList`;
- `==`, `equals()`, `hashCode()` y `toString()`;
- bytecode, JDK, JVM y límites de portabilidad;
- checked/unchecked y flujo `try/catch/finally/throw/throws`;
- SRP, DIP y reconocimiento de OCP;
- listas doble, circular y ordenada;
- iteradores y eliminación segura;
- DDL, DML, TCL y DCL;
- consultas y cambios SQL seguros;
- PK, FK, `NOT NULL`, `UNIQUE` y `CHECK`;
- validación complementaria entre Java y base de datos.

## Pendientes trasladados

- programa Java autónomo con archivos separados;
- manipulación de archivos y excepciones propias integradas;
- práctica espaciada de excepciones;
- ordenación y complejidades;
- asociativa frente a débil;
- modelo relacional final completo;
- digitalización y supuestos de transmisión.

## Errores canónicos

```text
Consolidado = 39
Corregido = 15
Abierto = 5
Total = 59
```

## Continuación

`19/08/2026`: modelo de implementación + archivos y excepciones propias + enlace de datos/Ethernet.
