# Cierre del Día 11

## Java complementario + Listas avanzadas + SQL

**Fecha:** 18/08/2026<br>
**Estado:** **CERRADO Y APROBADO**<br>
**Evaluación:** **9,2/10**<br>
**Recuperación oral posterior:** completada<br>
**Tiempo real:** no cronometrado

---

# 1. Actividades realizadas

- [x] Recuperación inicial.
- [x] Arreglo frente a `ArrayList`.
- [x] `String`, `equals()` y `hashCode()`.
- [x] JVM, bytecode y portabilidad.
- [x] Checked frente a unchecked.
- [x] `try`, `catch`, `finally`, `throw` y `throws`.
- [x] Lectura y ejecución mental de código Java.
- [x] Lista doble.
- [x] Lista circular.
- [x] Lista ordenada.
- [x] Iterador y eliminación segura.
- [x] DDL, DML, TCL y DCL.
- [x] `SELECT`, `FROM`, `WHERE` y `ORDER BY`.
- [x] `INSERT`, `UPDATE` y `DELETE`.
- [x] Restricciones y cambios SQL seguros.
- [x] SOLID conceptual.
- [x] Caso integrador Java + estructuras + base de datos.
- [x] Evaluación final.
- [x] Recuperación oral de excepciones.

No se registra como evidencia del estudiante la ejecución automática de los ejemplos Java realizada durante la preparación del material.

---

# 2. Resultado

| Área | Máximo | Obtenido |
|---|---:|---:|
| Java | 3,00 | **2,50** |
| Listas avanzadas | 3,00 | **2,80** |
| SQL | 3,00 | **2,90** |
| Integración/SOLID | 1,00 | **1,00** |
| **Total** | **10,00** | **9,20** |

```text
Resultado = 9,2/10
Estado = APROBADO
Recuperación posterior = completada sin cambiar la nota
```

---

# 3. Evidencia formativa

- recuperación inicial: `11/12`;
- arreglo/`ArrayList`: `4/4`;
- igualdad: recuperación `3/3`;
- excepciones: casos de compilación `4/4` y flujo `4/4`;
- JVM: `3/4`;
- lista doble: `4/5`, con recuperación de complejidades `3/3`;
- lista circular: `4/5`, con recuperación `2/2`;
- lista ordenada: `5/5`;
- iterador: `6/6`;
- familias SQL: `9/9`;
- restricciones SQL: `5/5`;
- caso integrador: `7/8`;
- SOLID: `5/5`.

Los controles formativos no se promedian con el `9,2/10`; solo respaldan el avance registrado.

---

# 4. Fortalezas

1. Diferencia entre arreglo y `ArrayList`, incluido acceso por índice y redimensionamiento.
2. Contrato entre `equals()` y `hashCode()`.
3. JVM, bytecode y límites reales de “write once, run anywhere”.
4. Enlaces de lista doble y condición de cierre circular.
5. Lista ordenada, iteradores y complejidades de recorrido.
6. Clasificación DDL/DML/TCL/DCL.
7. SQL ejecutable con verificaciones y `WHERE` explícito.
8. Aplicación de SRP y DIP a `AgendaTrabajos`.
9. Validación en Java y restricciones simultáneas en la base.

---

# 5. Errores y recuperaciones

| Código | Tema | Situación observada | Estado al cierre |
|---|---|---|---|
| E-008 | DCL/TCL | antes era una brecha; `COMMIT` y `GRANT` se clasificaron correctamente en dos instancias | consolidado |
| E-018 | estructura dinámica/tipo | `ArrayList` redimensionable y uso de objetos/wrappers explicados nuevamente | consolidado |
| E-046 | comparación de `String` | el error inicial sobre `==` fue corregido y luego defendido | consolidado |
| E-056 | igualdad por identidad lógica | inicialmente se exigió misma instancia para dos trabajos con igual `id` | corregido |
| E-057 | excepciones y propagación | se generalizó la obligación de `throws` y el fallo de unchecked | corregido |
| E-058 | costo con referencias conocidas | se respondió `O(n)` aunque A, X y B ya estaban disponibles | corregido |
| E-059 | lista circular de un nodo | se indicó `null`; debe apuntarse a sí mismo | corregido |

Reglas retenidas:

```text
throw = lanzar ahora
throws = declarar posible propagación
checked = capturar o declarar
unchecked = sin obligación del compilador
```

```text
referencias ya disponibles → enlazar/desenlazar O(1)
buscar la posición o el nodo → O(n)
lista circular de un nodo → nodo.siguiente == nodo
```

---

# 6. Cobertura pendiente

- escritura autónoma de un programa Java completo con archivos separados;
- manipulación de archivos y excepciones propias integradas;
- práctica espaciada de checked/unchecked y `throw`/`throws`;
- algoritmos de ordenación y sus complejidades;
- modelo relacional final completo de la brecha E-050;
- asociativa frente a débil;
- digitalización, política y supuestos de transmisión.

---

# 7. Integración alcanzada

```text
captura Java
→ validación temprana
→ ArrayList/estructura local
→ igualdad por id
→ repositorio abstracto
→ sincronización
→ restricciones PK/FK/NOT NULL/CHECK en la base
→ UPDATE o DELETE con WHERE
```

La aplicación mejora la experiencia y rechaza errores tempranamente; la base conserva la integridad aunque otro cliente o proceso escriba por una vía diferente.

---

# 8. Decisión de cierre

- [x] Cerrado y aprobado.
- [x] Recuperación oral completada.
- [x] Errores y avances registrados.
- [x] Pendientes preservados sin declararlos dominados.
- [ ] Fusionado en `main`.

Justificación: la evaluación final alcanzó `9,2/10`; Java quedó aprobado con una precisión recuperada, Listas avanzadas y SQL mostraron evidencia sólida, y SOLID fue aplicado correctamente al caso.

---

# 9. Próxima jornada

```text
19/08/2026 — Día 12
→ modelo de implementación, componentes, nodos y despliegue
→ archivos y excepciones propias en Java
→ enlace de datos, Ethernet, LLC/MAC, tabla MAC y STP
```
