# Evaluación EFIP I — Día 11
## Java complementario + Listas avanzadas + SQL

**Fecha:** 18/08/2026  
**Tiempo sugerido:** 55–60 minutos  
**Puntaje:** 10 puntos  
**Aprobación orientativa:** 7/10

---

# Parte A — Java — 3 puntos

## 1. Arreglo y ArrayList — 0,5

Diferenciá tamaño, acceso, inserción y tipos admitidos.

## 2. Igualdad de objetos — 0,75

Explicá `==`, `equals()` y la regla de `hashCode()`.

## 3. Excepciones — 1,25

Diferenciá checked/unchecked y explicá `try`, `catch`, `finally`, `throw` y `throws`.

## 4. JVM — 0,5

Explicá el recorrido `.java → bytecode → JVM` y una limitación de la portabilidad.

---

# Parte B — Listas avanzadas — 3 puntos

## 5. Comparación — 1

Diferenciá lista simple, doble, circular y ordenada.

## 6. Enlaces — 1

Partiendo de `A ↔ B`, insertá `X` entre ambos e indicá las cuatro referencias.

## 7. Circular e iterador — 1

Explicá cómo termina una vuelta circular y qué hacen `hasNext()` y `next()`.

---

# Parte C — SQL — 3 puntos

## 8. Familias — 0,75

Clasificá `CREATE`, `ALTER`, `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `COMMIT` y `GRANT`.

## 9. Consulta — 0,75

Escribí un `SELECT` que muestre trabajos `PENDIENTE` ordenados por prioridad.

## 10. Cambios seguros — 1,5

Escribí:

- un `INSERT`;
- un `UPDATE` de una sola fila;
- un `DELETE` de una sola fila;
- la verificación previa que realizarías.

---

# Parte D — Integración y SOLID — 1 punto

Elegí dos principios SOLID, definilos en una oración y aplicalos al caso `AgendaTrabajos`. Después explicá qué validación debe existir tanto en Java como en la base.

---

# Criterios

- precisión conceptual;
- asignaciones de enlaces correctas;
- código y SQL completos;
- `WHERE` explícito;
- decisiones justificadas;
- no atribuir como oficial lo que se trabajó como complemento.
